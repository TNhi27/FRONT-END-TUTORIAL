import React from 'react';
import Loading from '../myconponent/Loading';

function Works(props) {
    return (
        <div className='works' id='works'>
           <Loading/>
        </div>
    );
}

export default Works;